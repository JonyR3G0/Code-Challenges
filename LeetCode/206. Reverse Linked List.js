/**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.next = (next===undefined ? null : next)
 * }
 * 
 * EXAMPLE=>
 * Input: head = [1,2,3,4,5]
 * Output: [5,4,3,2,1]
 */

/**
 * @param {ListNode} head
 * @return {ListNode}
 */
var reverseList = function(head) {
    //Solucion iterativa?
    const headVal = []
    const reversed = {}
    //Iterar hasta que no haya un nodo siguiente
    while (this.next === undefined) {
        //Leer valor de nodo y guardarlo en el array headVal
    }
    for (let i = 0; i < headVal.length; i++) {
        const element = array[i];
        //Crear linked list a partir del head val
    }
    return reversed
};